spring.application.name=hospital

spring.datasource.url=jdbc:postgresql://localhost:5432/internhospital
#this internschool was uesd for the to create the database from the calling in postman and create in the pg-admin and create the data in the pg-admin from the postman  
server.port=8088

spring.datasource.username=postgres
spring.datasource.password=root

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

